# Phase 0 baseline
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.5
# Phase 3 additions: feature engineering + model inference
scikit-learn>=1.3
joblib>=1.3
numpy>=1.26
PyMuPDF>=1.23
# Phase 5: cross-application graph
networkx>=3.0
# §6.D2: the risk service runs the forensic analyzer in-process, so it needs the OCR
# stack for the re-OCR cross-check. Without Tesseract installed the check no-ops.
pytesseract>=0.3.10
Pillow>=10
